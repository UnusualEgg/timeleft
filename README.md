Tells you how much time left until next class

(this is the default)
makes a wasm32-unknown-emscripten library for the web

and timeleft executable for the commamnd line which uses sigalarm for timing
